import { BlankUrlDirective } from './blank-url.directive';

describe('BlankUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new BlankUrlDirective();
    expect(directive).toBeTruthy();
  });
});
